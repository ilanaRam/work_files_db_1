Name: 
Files travers in python

Description: 
This project will deal with files, will work with methos that used in a work with files in python. 
Will work with python db dict that will hold a files stracture
Will work with external DB - based on SQL to store files
Will be use pytest framework to run tests, the cmd: python -m pytest .\test_travers_path_get_files.py -v -s


### Requirements
- Python 3.x
- pip (Python package manager)


### Installation Steps

1. Clone the repository:
   git clone https://github.com/ilanaRam/work_files_db_1.git
   cd work_files_db_1

### Set up a virtual environment
python -m venv venv
source venv/bin/activate  # On Windows, use `venv\Scripts\activate`
Comments:
adde .gitignore added to the project

Check that was selected correct Python Interpreter: 
(if in VSCode): Cltrl + Ship + P will be opened a window - inside it select the the venv interpreter and not the difault python installation  

Created requirements.txt file: 
by cmd:  git freeze > requirements.txt 
 
To install all required packages do: 
pip install -r requirements.txt